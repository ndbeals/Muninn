import allResolvers from './resolvers';
import userResolvers from './user';
import notificationResolvers from './notification';

const resolvers = [allResolvers, userResolvers, notificationResolvers];

export default resolvers;
